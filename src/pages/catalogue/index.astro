---
import HatchCatalogueGrid from "@/components/sections/hatch-catalogue-grid.astro";
import HatchCta from "@/components/sections/hatch-cta";
import { SITE_DESCRIPTION } from "@/consts";
import DefaultLayout from "@/layouts/DefaultLayout.astro";
import { getAllFormations } from "@/lib/get-formations";

const formations = getAllFormations().map((entry) => ({
  slug: entry.slug,
  title: entry.title,
  subtitle: entry.subtitle,
  description: entry.description,
  image: entry.image,
  duration: entry.duration,
  format: entry.format,
  price: entry.price,
}));
---

<DefaultLayout title="Catalogue des formations" description={SITE_DESCRIPTION}>
  <HatchCatalogueGrid
    title="Nos formations en langues"
    subtitle="Des parcours linguistiques adaptés à tous les niveaux, tous les publics et tous les objectifs."
    items={formations}
  />
  <HatchCta
    title={"Besoin d’un format sur mesure\npour votre equipe ou votre projet ?"}
    ctaLabel="Demander une proposition"
    ctaHref="/contact"
  />
</DefaultLayout>
